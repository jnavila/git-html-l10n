`add.ignoreErrors`::
`add.ignore-errors` (deprecated 弃用)::
	当某些文件由于索引错误而无法添加时，使用 `git add` 以继续添加文件。
ifdef::git-add[]
	与`--ignore-errors`选项作用相同。
endif::git-add[]
ifndef::git-add[]
	等同于 linkgit:git-add[1] 的 `--ignore-errors` 选项。
endif::git-add[]
	`add.ignore-errors` 已经被弃用，
	因为它不符合配置变量的常规命名约定。

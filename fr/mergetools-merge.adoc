`araxis`;;           Utiliser Araxis Merge (nécessite une session graphique)
`bc`;;               Utiliser Beyond Compare (nécessite une session graphique)
`bc3`;;              Utiliser Beyond Compare (nécessite une session graphique)
`bc4`;;              Utiliser Beyond Compare (nécessite une session graphique)
`codecompare`;;      Utiliser Code Compare (nécessite une session graphique)
`deltawalker`;;      Utiliser DeltaWalker (nécessite une session graphique)
`diffmerge`;;        Utiliser DiffMerge (nécessite une session graphique)
`diffuse`;;          Utiliser Diffuse (nécessite une session graphique)
`ecmerge`;;          Utiliser ECMerge (nécessite une session graphique)
`emerge`;;           Utiliser Emerge d'Emacs
`examdiff`;;         Utiliser ExamDiff Pro (nécessite une session graphique)
`guiffy`;;           Utiliser l'outil Diff de Guiffy (nécessite une session graphique)
`gvimdiff`;;         Utiliser gVim (nécessite une session graphique) avec une disposition personnalisée (voir la section `BACKEND SPECIFIC HINTS` de `git help mergetool`)
`gvimdiff1`;;        Utiliser gVim (nécessite une session graphique) avec une disposition à 2 volets (LOCAL et REMOTE)
`gvimdiff2`;;        Utiliser gVim (nécessite une session graphique) avec une disposition à 3 volets (LOCAL, MERGED et REMOTE)
`gvimdiff3`;;        Utiliser gVim (nécessite une session graphique) où seul le fichier MERGED est affiché
`kdiff3`;;           Utiliser KDiff3 (nécessite une session graphique)
`meld`;;             Utiliser Meld (nécessite une session graphique) avec `auto merge` optionnel (voir la section `CONFIGURATION` de `git help mergetool`)
`nvimdiff`;;         Utiliser Neovim avec une disposition personnalisée (voir la section `BACKEND SPECIFIC HINTS` de `git help mergetool`)
`nvimdiff1`;;        Utiliser Neovim avec une disposition à 2 volets (LOCAL et REMOTE)
`nvimdiff2`;;        Utiliser Neovim avec une disposition à 3 volets (LOCAL, MERGED et REMOTE)
`nvimdiff3`;;        Utiliser Neovim où seul le fichier MERGED est affiché
`opendiff`;;         Utiliser FileMerge (nécessite une session graphique)
`p4merge`;;          Utiliser HelixCore P4Merge (nécessite une session graphique)
`smerge`;;           Utiliser Sublime Merge (nécessite une session graphique)
`tkdiff`;;           Utiliser TkDiff (nécessite une session graphique)
`tortoisemerge`;;    Utiliser TortoiseMerge (nécessite une session graphique)
`vimdiff`;;          Utiliser Vim avec une disposition personnalisée (voir la section `BACKEND SPECIFIC HINTS` de `git help mergetool`)
`vimdiff1`;;         Utiliser Vim avec une disposition à 2 volets (LOCAL et REMOTE)
`vimdiff2`;;         Utiliser Vim avec une disposition à 3 volets (LOCAL, MERGED et REMOTE)
`vimdiff3`;;         Utiliser Vim où seul le fichier MERGED est affiché
`vscode`;;           Utiliser Visual Studio Code (nécessite une session graphique)
`winmerge`;;         Utiliser WinMerge (nécessite une session graphique)
`xxdiff`;;           Utiliser xxdiff (nécessite une session graphique)
